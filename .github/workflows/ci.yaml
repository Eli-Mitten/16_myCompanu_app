name: Application CI

on:
  push:
    branches:
      - main

jobs:
  inherit-workflow:
    uses: /Eli-Mitten/gitactions-reusable-workflow/.github/workflows/gitactions-reusable-workflow.yaml@main
    with:
      node-version: '20'
